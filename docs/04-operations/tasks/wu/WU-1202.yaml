id: WU-1202
title: 'Bug: orchestrate:initiative missing spawn XML when checkpoint not auto-enabled'
lane: 'Framework: CLI'
type: bug
status: ready
priority: P2
created: 2026-01-29
code_paths:
  - packages/@lumenflow/cli/src/orchestrate-initiative.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/__tests__/orchestrate-initiative.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1202.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: When checkpoint mode is not auto-enabled (<= 3 pending WUs and <= 2 waves), orchestrate:initiative outputs only the execution plan text with a misleading message 'Copy the spawn commands above' - but no spawn XML is actually output. The spawn XML output only exists in the checkpoint code path (formatCheckpointOutput), not the execution plan path (formatExecutionPlan).
acceptance:
  - Spawn XML output in execution plan path when not dry-run
  - OR always use checkpoint mode when actually executing
  - OR fix misleading message to say 'use -c flag or wu:spawn'
