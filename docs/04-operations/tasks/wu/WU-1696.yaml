id: WU-1696
title: 'Strict config completion: remove core/cli false overrides'
lane: 'Framework: Core Validation'
type: refactor
status: ready
priority: P1
created: 2026-02-15
code_paths:
  - packages/@lumenflow/core/tsconfig.json
  - packages/@lumenflow/cli/tsconfig.json
  - packages/@lumenflow/core/__tests__/tsconfig-inheritance.test.ts
tests:
  manual:
    - pnpm --filter @lumenflow/core exec tsc --noEmit --strict --pretty false
    - pnpm --filter @lumenflow/cli exec tsc --noEmit --strict --pretty false
  unit:
    - packages/@lumenflow/core/__tests__/tsconfig-inheritance.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1696.done
dependencies: []
initiative: INIT-022
phase: 10
spec_refs:
  - docs/04-operations/tasks/initiatives/INIT-022.yaml
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: strict compilation for @lumenflow/core and @lumenflow/cli currently succeeds under --strict, but both package tsconfigs still explicitly override strictNullChecks/noImplicitAny to false. Problem: policy and config remain inconsistent with full strict-mode completion. Solution: remove the false overrides, tighten policy tests first, and verify strict compilation remains green.'
acceptance:
  - 'packages/@lumenflow/core/tsconfig.json no longer sets strictNullChecks: false or noImplicitAny: false'
  - 'packages/@lumenflow/cli/tsconfig.json no longer sets strictNullChecks: false or noImplicitAny: false'
  - packages/@lumenflow/core/__tests__/tsconfig-inheritance.test.ts enforces non-deferral for core/cli strictNullChecks
  - pnpm --filter @lumenflow/core exec tsc --noEmit --strict --pretty false passes
  - pnpm --filter @lumenflow/cli exec tsc --noEmit --strict --pretty false passes
