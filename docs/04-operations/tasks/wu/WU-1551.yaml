id: WU-1551
title: Fix floating promises and replace setInterval polling
lane: 'Framework: Core Lifecycle'
type: refactor
status: ready
priority: P1
created: 2026-02-09
code_paths:
  - packages/@lumenflow/cli/src/mem-inbox.ts
  - packages/@lumenflow/core/src/patrol-loop.ts
tests:
  manual:
    - Run rg setInterval.*async packages/@lumenflow/*/src/ --type ts and verify zero unguarded
      patterns.
  unit:
    - packages/@lumenflow/cli/src/__tests__/mem-inbox-watch.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1551.done
dependencies: []
initiative: INIT-020
phase: 11
spec_refs:
  - lumenflow://plans/polished-frolicking-whale.md
risks: []
notes: Track K from enterprise hardening initiative. Covers plan findings 24, 25, 37.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: mem-inbox.ts uses async callback in setInterval (promise floats).
  patrol-loop.ts has promise chain in setTimeout without terminal .catch(). mem-inbox.ts polls
  instead of using chokidar (already installed). Problem: unhandled promise rejections and
  unnecessary CPU wakes. Solution: replace setInterval with chokidar.watch(), add .catch() to
  patrol-loop.ts.'
acceptance:
  - mem-inbox.ts uses chokidar.watch() instead of setInterval polling for watch mode.
  - patrol-loop.ts promise chain has terminal .catch() error handler.
  - No unguarded async callbacks in setInterval or setTimeout patterns remain.
claimed_mode: worktree
approved_by:
  - tom@hellm.ai
approved_at: 2026-02-09T23:52:46.206Z
