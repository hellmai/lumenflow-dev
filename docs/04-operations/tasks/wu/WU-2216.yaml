id: WU-2216
title: Wire runtime to provide available manifests for root-key migration version resolution
lane: 'Framework: Core Validation'
type: bug
status: in_progress
priority: P2
created: 2026-02-26
code_paths:
  - packages/@lumenflow/kernel/src/runtime/kernel-runtime.ts
  - packages/@lumenflow/kernel/src/__tests__/runtime.test.ts
tests:
  manual:
    - Create legacy workspace with software_delivery + no SD pin and verify runtime error includes
      concrete --version from available manifest
    - Simulate unavailable manifest and verify remediation falls back to --version latest
  unit:
    - packages/@lumenflow/kernel/src/__tests__/runtime.test.ts
    - packages/@lumenflow/kernel/src/__tests__/kernel.schemas.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2216.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
claimed_mode: worktree
assigned_to: tom@hellm.ai
claimed_at: 2026-02-26T14:32:05.139Z
worktree_path: /home/USER/source/hellmai/os/worktrees/framework-core-validation-wu-2216
session_id: 6bf623de-c21b-4d5a-953d-67e1ad96e748
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
approved_by:
  - tom@hellm.ai
approved_at: 2026-02-26T14:32:05.147Z
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: validateWorkspaceRootKeys now supports availableManifests for migration remediation
  version lookup, but initializeKernelRuntime currently only passes pinned manifests. In legacy
  workspaces (software_delivery present, SD not pinned), the runtime path falls back to --version
  latest instead of a concrete manifest version. Wire runtime to supply available manifests and add
  integration coverage so runtime error messages use manifest-derived versions.
acceptance:
  - initializeKernelRuntime passes available manifests into validateWorkspaceRootKeys
  - Legacy workspace remediation message includes concrete SD version from manifest instead of
    --version latest
  - Runtime tests cover both resolved-version and fallback behavior when manifest is unavailable
