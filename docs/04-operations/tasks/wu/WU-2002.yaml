id: WU-2002
title: Stabilize lane-health test under full CLI test matrix
lane: 'Framework: Core Validation'
type: bug
status: ready
priority: P2
created: 2026-02-22
code_paths:
  - packages/@lumenflow/cli/__tests__/lane-health.test.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/__tests__/lane-health.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2002.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Full wu:done gate runs intermittently fail in packages/@lumenflow/cli/__tests__/lane-health.test.ts (detectCoverageGaps expectation) when executed inside the full turbo test matrix, while isolated runs pass. Stabilize mocking/import state so the suite is deterministic in full runs.
acceptance:
  - lane-health detectCoverageGaps tests pass deterministically in full turbo test matrix
  - pnpm --filter @lumenflow/cli test no longer flakes on lane-health expectations
  - Regression coverage documents the stabilized behavior
