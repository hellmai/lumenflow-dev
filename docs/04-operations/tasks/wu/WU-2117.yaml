id: WU-2117
title: Migrate throw new Error() to createError() in @lumenflow/cli (batch)
lane: 'Framework: CLI WU Commands'
type: refactor
status: ready
priority: P1
created: 2026-02-24
code_paths:
  - packages/@lumenflow/cli/src/
tests:
  manual:
    - Verify zero throw new Error() occurrences in packages/@lumenflow/cli/src/ via grep
    - Verify error-pattern-guard baseline for cli is 0
    - Verify pnpm test --filter=@lumenflow/cli passes
  unit:
    - packages/@lumenflow/core/src/__tests__/error-pattern-guard.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2117.done
dependencies: []
initiative: INIT-037
phase: 3
blocked_by:
  - WU-2111
  - WU-2116
spec_refs:
  - /home/tom/.claude/plans/wild-foraging-taco.md
risks: []
notes: 'Single-session override: ~25 files modified in @lumenflow/cli but all are mechanical throw
  new Error(msg) -> throw createError(ErrorCodes.X, msg) replacements. Each change is 1-3 lines,
  pattern is identical. No structural changes. Complexity: Low (uniform search-replace with new
  ErrorCodes as needed). Shallow multi-file exception per wu-sizing-guide.md §1.2.'
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'With AST guard WU-2111 in place preventing regression, migrate all throw new Error()
  usages in @lumenflow/cli to createError(ErrorCodes.*) from error-handler.ts. Pattern: throw new
  Error(msg) -> throw createError(ErrorCodes.X, msg). Add missing ErrorCodes as needed. After batch
  verify guard baseline decreases. WU-2116 (core) runs first to avoid cross-package dependency
  issues.'
acceptance:
  - Zero throw new Error() occurrences in packages/@lumenflow/cli/src/ (excluding tests)
  - error-pattern-guard baseline for cli decreases to 0
  - All new ErrorCodes are semantically named
  - pnpm test --filter=@lumenflow/cli passes
