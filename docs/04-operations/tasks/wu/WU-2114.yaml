id: WU-2114
title: Extend path-literal-guard to ban raw file extension literals like .yaml .json .md
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P1
created: 2026-02-24
code_paths:
  - packages/@lumenflow/core/src/__tests__/path-literal-guard.test.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/core/src/__tests__/path-literal-guard.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2114.done
dependencies: []
initiative: INIT-037
phase: 2
blocked_by:
  - WU-2109
  - WU-2113
spec_refs:
  - /home/USER/.claude/plans/wild-foraging-taco.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "Raw file extension strings (.yaml, .yml, .json, .md, .ts, .js) are used directly in
  endsWith() and similar checks instead of FILE_EXTENSIONS constants from wu-paths-constants.ts. Add
  banned rule to path-literal-guard that matches bare extension strings (only .ext patterns, not
  paths containing extensions). Allowlist wu-paths-constants.ts. Catches: .endsWith('.yaml'),
  .endsWith('.json'), startsWith('.') extension checks, etc."
acceptance:
  - path-literal-guard extended with file extension banned pattern (matches bare .yaml, .yml, .json,
    .md, .ts, .js strings)
  - 'Pattern is precise: only bare extension strings not paths that happen to contain extensions'
  - Allowlist excludes wu-paths-constants.ts (canonical FILE_EXTENSIONS definition)
  - Guard catches .endsWith('.yaml') style literals in test run
  - pnpm test --testNamePattern=path-literal-guard passes
