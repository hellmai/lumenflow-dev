id: WU-1800
title: Migrate git tools (git_status/diff/log/branch) to runtime pack execution
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P1
created: 2026-02-18
code_paths:
  - packages/@lumenflow/mcp/src/tools/parity-tools.ts
  - packages/@lumenflow/packs/software-delivery/tool-impl/git-tools.ts
tests:
  manual:
    - Each git tool produces identical output via runtime path vs CLI path
  unit:
    - packages/@lumenflow/mcp/src/__tests__/runtime-tool-resolver.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1800.done
dependencies: []
initiative: INIT-030
phase: 3
blocked_by:
  - WU-1798
spec_refs:
  - /home/tom/.claude/plans/adaptive-finding-adleman.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: git_status, git_diff, git_log, git_branch MCP tools shell out via runCliCommand. Problem: these 4 call sites use shell-outs. The pack already has git:status in tool-impl. Solution: extend pack git-tools.ts, register in-process handlers, reconcile naming.'
acceptance:
  - git_status, git_diff, git_log, git_branch route through runtime.executeTool()
  - runCliCommand is NOT called for any of these 4 tools
  - Existing pack git:status tool reconciled with MCP git_status
