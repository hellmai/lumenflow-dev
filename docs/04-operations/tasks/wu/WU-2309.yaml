id: WU-2309
title: Fix wu:brief --evidence-only false 'Recorded' message when evidence is skipped
lane: 'Framework: CLI WU Commands'
type: bug
status: ready
priority: P2
created: 2026-03-03
code_paths:
  - packages/@lumenflow/cli/src/wu-spawn-strategy-resolver.ts
  - packages/@lumenflow/cli/__tests__/wu-spawn-strategy-resolver.test.ts
tests:
  manual:
    - Run wu:brief --id WU-2309 --evidence-only from a non-claimed context and verify output reports skipped evidence (not Recorded).
    - Run wu:brief --id WU-2309 --evidence-only from claimed context and verify output reports recorded evidence.
  unit:
    - packages/@lumenflow/cli/__tests__/wu-spawn-strategy-resolver.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2309.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "Context: wu:brief --evidence-only can print 'Recorded wu:brief evidence' even when context gating skips the write. Problem: users receive a false-success signal and may bypass with --force unnecessarily. Solution: return explicit record outcome from evidence writer and emit mode-aware output (recorded vs skipped, with actionable context)."
acceptance:
  - When evidence is skipped by context gate, wu:brief --evidence-only prints a skip message instead of 'Recorded'.
  - When evidence is written, wu:brief --evidence-only continues to print a successful recording message.
  - Add/update unit tests covering both recorded and skipped evidence-only outcomes.
