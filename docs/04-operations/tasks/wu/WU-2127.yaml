id: WU-2127
title: Split monolithic enforcement-checks module into focused path-utils, config-resolver, git-status-parser, dirty-guard modules
lane: 'Framework: Core Lifecycle'
type: refactor
status: ready
priority: P2
created: 2026-02-24
code_paths:
  - packages/@lumenflow/cli/src/
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/src/__tests__/
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2127.done
dependencies: []
initiative: INIT-037
phase: 4
blocked_by:
  - WU-2116
spec_refs:
  - /home/USER/.claude/plans/wild-foraging-taco.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'The enforcement-checks module has grown into a monolith mixing path utilities, config resolution, git status parsing, and dirty state checking. Split into 4 focused modules: path-utils.ts, config-resolver.ts, git-status-parser.ts, dirty-guard.ts. Each module has a single responsibility and clear boundaries. Allows individual modules to be tested and imported independently.'
acceptance:
  - enforcement-checks split into 4 focused modules
  - Each module has single responsibility (path util, config, git status, dirty check)
  - All existing callers updated to import from correct sub-module
  - pnpm test --filter=@lumenflow/cli passes
