id: WU-1981
title: Wire runtime control-plane sync lifecycle
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P0
created: 2026-02-21
code_paths:
  - packages/@lumenflow/surfaces/http/server.ts
  - packages/@lumenflow/surfaces/http/control-plane-event-subscriber.ts
  - packages/@lumenflow/surfaces/__tests__/control-plane-forwarding.test.ts
tests:
  manual:
    - HTTP surface emits forwarding diagnostics when control plane is enabled
  unit:
    - packages/@lumenflow/surfaces/__tests__/control-plane-forwarding.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1981.done
dependencies: []
initiative: INIT-033
phase: 3
blocked_by:
  - WU-1980
spec_refs:
  - docs/04-operations/plans/INIT-033-workspace-first-hard-cut-boots.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Wire runtime/surfaces integration for control-plane pull/push operations (policy pull, heartbeat, evidence/event forwarding) via sync port.
acceptance:
  - Runtime calls sync port pull and push operations using configured intervals
  - Policy pull can tighten local policy decisions
  - Network/auth failures emit actionable diagnostics without unsafe fallback
