id: WU-1231
title: Parallelize dependency validation in wu:spawn to reduce latency
lane: 'Framework: Core'
type: bug
status: ready
priority: P2
created: 2026-01-30
code_paths:
  - packages/@lumenflow/core/src/dependency-validator.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/core/src/__tests__/dependency-validator.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1231.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: wu:spawn takes 5+ minutes before generating prompt because validateSpawnDependencies() validates packages sequentially with await import(). Each import takes 1-2s, done one at a time. Should use Promise.all() for parallel validation.
acceptance:
  - dependency-validator.ts uses Promise.all() for parallel package validation
  - wu:spawn prompt generation completes in under 5 seconds
  - existing tests continue to pass
