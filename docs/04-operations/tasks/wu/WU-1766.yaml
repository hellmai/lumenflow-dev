id: WU-1766
title: 'Fix wu:claim --cloud: missing cloud-mode bypass before ensureOnMain'
lane: 'Framework: CLI WU Commands'
type: bug
status: in_progress
priority: P1
created: 2026-02-17
code_paths:
  - packages/@lumenflow/cli/src/wu-claim.ts
tests:
  manual:
    - "Verify wu:claim --cloud works from a non-main branch by running: git checkout -b
      claude/test-claim && pnpm wu:claim --cloud --id WU-TEST --lane 'Framework: Core'"
  unit:
    - packages/@lumenflow/cli/src/__tests__/wu-claim.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1766.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "wu:create --cloud has a cloud-mode bypass that skips ensureOnMain(), allowing it to
  work from agent branches (claude/*, codex/*). wu:claim --cloud does NOT have this bypass — it
  calls ensureOnMain() before the cloud check, causing it to fail on non-main branches. This creates
  a chicken-and-egg problem in cloud sessions: can't claim a WU (needed to edit files), can't edit
  files (hooks require a claimed WU)."
acceptance:
  - wu:claim --cloud bypasses ensureOnMain like wu:create --cloud does
  - wu:claim --cloud works from claude/* and codex/* branches
  - Test covering wu:claim cloud mode from non-main branch
claimed_mode: worktree
worktree_path: /home/USER/source/hellmai/os/worktrees/framework-cli-wu-commands-wu-1766
claimed_at: 2026-02-17T10:26:31.969Z
baseline_main_sha: ae61a3000858df741192685d29cfd03516354d52
session_id: 2f8cbf94-50c0-421d-96fb-b304a59a0ae8
approved_by:
  - tom@hellm.ai
approved_at: 2026-02-17T10:26:31.974Z
