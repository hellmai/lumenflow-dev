id: WU-1489
title: Fix pre-existing lane-lock test regressions blocking gates
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P2
created: 2026-02-06
code_paths:
  - packages/@lumenflow/core/src/lane-lock.ts
tests:
  manual:
    - Run pnpm --filter @lumenflow/core exec vitest run src/__tests__/lane-lock.test.ts and verify
      lane-lock regressions are resolved on main
  unit:
    - packages/@lumenflow/core/src/__tests__/lane-lock.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1489.done
dependencies: []
spec_refs:
  - lumenflow://docs/04-operations/tasks/wu/WU-1487.yaml
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Restore failing lane-lock unit tests on main caused by lock_policy behavior drift so
  global gates run cleanly without skip-gates.
acceptance:
  - Targeted @lumenflow/core lane-lock test suite passes on main; global gates no longer fail
    because of lane-lock regressions
