id: WU-2017
title: Replace invariant type dispatch with strategy pattern
lane: 'Framework: Core Lifecycle'
type: refactor
status: ready
priority: P2
created: 2026-02-22
code_paths:
  - packages/@lumenflow/core/src/invariants-runner.ts
  - packages/@lumenflow/core/src/ports/
tests:
  manual:
    - Run pnpm gates from worktree — all tests pass
  unit:
    - packages/@lumenflow/core/src/__tests__/invariants-runner.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2017.done
dependencies: []
initiative: INIT-034
phase: 3
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Replace the 6+ else-if chain in invariants-runner.ts (743 lines) with InvariantChecker strategy pattern. Each invariant type (required-file, forbidden-file, mutual-exclusivity, forbidden-pattern, required-pattern, forbidden-import, wu-automated-tests) gets its own class implementing a common interface. New invariant types can be added without modifying existing code (OCP).
acceptance:
  - InvariantChecker interface defined with validate() method
  - Each invariant type has dedicated implementation class
  - No if-else or switch dispatch on invariant type remains
  - Adding a new invariant type requires only a new class + registry entry
  - All existing invariant tests pass
