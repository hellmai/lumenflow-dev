id: WU-1005
title: Add lumenflow init scaffolding command
lane: CLI
type: feature
status: done
priority: P2
created: 2026-01-18
code_paths:
  - packages/@lumenflow/cli/src/init.ts
tests:
  manual:
    - npx @lumenflow/cli init creates .lumenflow.yaml
    - npx @lumenflow/cli init creates CLAUDE.md template
  unit:
    - packages/@lumenflow/cli/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1005.done
dependencies:
  - WU-1002
spec_refs:
  - internal
risks:
  - Template content may need iteration based on user feedback
notes: Completed per acceptance criteria.
requires_review: false
assigned_to: tom@hellm.ai
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
exposure: backend-only
description: |
  Add `lumenflow init` command to scaffold new projects.

  Context: New projects adopting LumenFlow need configuration and docs.
  Problem: Users must manually copy files from other repos (error-prone).
  Solution: CLI init command that scaffolds everything needed.

  Scaffolds:
  1. .lumenflow.yaml - project configuration
  2. CLAUDE.md - development guide template
  3. AGENTS.md - agent context template
  4. .lumenflow/ - stamps directory
  5. docs/04-operations/tasks/wu/ - WU storage directory

  Usage:
  ```bash
  npx @lumenflow/cli init
  # or after install:
  pnpm lumenflow init
  ```
acceptance:
  - lumenflow init creates .lumenflow.yaml with sensible defaults
  - lumenflow init creates CLAUDE.md template
  - lumenflow init creates AGENTS.md template
  - lumenflow init creates .lumenflow/ directory
  - lumenflow init creates docs/ structure for WUs
  - lumenflow init --force overwrites existing files
  - pnpm format, lint, typecheck → PASS
claimed_mode: worktree
worktree_path: worktrees/cli-wu-1005
claimed_at: 2026-01-18T18:15:51.324Z
baseline_main_sha: 9416c26ff0e303a86349ae6fc66db41999bbaced
session_id: cff097c2-d00b-48f7-9d30-27c00a680135
approved_by:
  - tom@hellm.ai
approved_at: 2026-01-18T18:15:51.337Z
locked: true
completed_at: 2026-01-18T18:19:31.636Z
