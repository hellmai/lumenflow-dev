id: WU-1308
title: Honor git.requireRemote and improve missing lane-inference errors
lane: 'Framework: Core'
type: bug
status: ready
priority: P1
created: 2026-02-01
code_paths:
  - packages/@lumenflow/core/src/micro-worktree.ts
  - packages/@lumenflow/core/src/lane-checker.ts
  - packages/@lumenflow/core/src/lumenflow-config.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/core/src/__tests__/micro-worktree-require-remote.test.ts
    - packages/@lumenflow/core/src/__tests__/lane-inference-error.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1308.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Fix two onboarding blockers: (1) micro-worktree operations must respect git.requireRemote=false and skip origin fetch/merge when remote is missing, enabling local-only wu:create/wu:claim; (2) when a sub-lane is used but .lumenflow.lane-inference.yaml is missing or invalid, lane-checker should emit an explicit error naming the missing file and a clear fix command.'
acceptance:
  - micro-worktree skips origin fetch/merge when git.requireRemote=false (local-only wu:create/wu:claim succeed without origin)
  - Sub-lane validation error explicitly calls out missing/invalid .lumenflow.lane-inference.yaml and suggests generation command
  - Unit tests cover requireRemote enforcement and lane-inference error message
