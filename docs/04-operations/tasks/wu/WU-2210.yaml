id: WU-2210
title: Migration remediation command missing --version and hardcodes --source local
lane: 'Framework: Core Validation'
type: bug
status: ready
priority: P1
created: 2026-02-26
code_paths:
  - packages/@lumenflow/kernel/src/kernel.schemas.ts
  - packages/@lumenflow/kernel/src/__tests__/runtime.test.ts
tests:
  manual:
    - Copy the remediation command from a legacy workspace boot error and run it verbatim
  unit:
    - packages/@lumenflow/kernel/src/__tests__/runtime.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2210.done
dependencies: []
initiative: INIT-045
phase: 4
spec_refs:
  - /home/USER/.claude/plans/init-045-multi-pack-config.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'WU-2200 fixed remediation from config:set to pack:install but command is still not executable: (1) pack:install requires --version (pack-install.ts:692) but error omits it; (2) --source local hardcodes monorepo layout (DEFAULT_PACKS_ROOT at pack-install.ts:614) which fails for external consumers. Fix to use --source registry with valid version.'
acceptance:
  - Remediation command includes all required pack:install flags (--id, --source, --version) so it executes without missing-option errors
  - Remediation command works for external consumers (not monorepo-biased --source local)
  - Test asserts the command includes --version and validates structural completeness (all required flags present)
