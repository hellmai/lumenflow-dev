id: WU-1064
title: Fix CLI silent failure - async main() without catch handler
lane: 'Framework: CLI'
type: bug
status: ready
priority: P2
created: 2026-01-22
code_paths:
  - packages/@lumenflow/cli/src/initiative-*.ts
tests:
  manual:
    - Run pnpm wu:claim --help and verify output
  unit:
    - packages/@lumenflow/cli/src/__tests__/cli-entry-point.test.ts
  e2e: []
artifacts:
  - .beacon/stamps/WU-1064.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Fix critical bug where async main() is called without .catch() handler, causing silent failures in 21 CLI commands
acceptance:
  - All CLI entry points have .catch() error handler
  - pnpm wu:claim --help shows usage
  - pnpm wu:done --id INVALID shows error not silent failure
