id: WU-1761
title: 'Harden sandbox: restrict bwrap root mount, fix lane lock TOCTOU, fix MCP schemas'
lane: 'Framework: Core Lifecycle'
type: bug
status: ready
priority: P1
created: 2026-02-17
code_paths:
  - packages/@lumenflow/kernel/src/sandbox/bwrap-invocation.ts
  - packages/@lumenflow/packs/software-delivery/tool-impl/lane-lock.ts
  - packages/@lumenflow/surfaces/mcp/server.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/kernel/src/__tests__/sandbox.test.ts
    - packages/@lumenflow/packs/software-delivery/__tests__/lane-lock.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1761.done
dependencies: []
initiative: INIT-029
phase: 6
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: noreply@anthropic.com
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: bwrap sandbox mounts entire root FS read-only (--ro-bind / /) with minimal deny overlays — weak isolation. Lane lock stale takeover has TOCTOU race allowing dual acquisition. MCP task:claim/task:complete advertise wrong input schemas (id-only vs actual required fields).
acceptance:
  - bwrap uses allowlist mount strategy instead of deny-overlay on full root
  - Lane lock stale takeover uses atomic compare-and-swap pattern
  - MCP input_schema matches actual handler requirements for all tools
  - Security-focused tests for sandbox escape, lock contention, MCP validation
