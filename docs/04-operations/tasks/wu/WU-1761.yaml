id: WU-1761
title: 'Harden sandbox: allowlist mounts and reserved .lumenflow scope enforcement'
lane: 'Framework: Core Lifecycle'
type: bug
status: ready
priority: P1
created: 2026-02-17
code_paths:
  - packages/@lumenflow/kernel/src/sandbox/bwrap-invocation.ts
  - packages/@lumenflow/kernel/src/tool-host/tool-host.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/kernel/src/__tests__/sandbox.test.ts
    - packages/@lumenflow/kernel/src/__tests__/tool-host.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1761.done
dependencies:
  - WU-1767
blocked_by:
  - WU-1767
initiative: INIT-029
phase: 6
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: noreply@anthropic.com
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Sandbox bwrap currently relies on broad root read-only mounting with deny overlays.
  This WU narrows sandbox mount exposure via allowlist strategy and enforces reserved .lumenflow/**
  scope boundaries in kernel scope validation so pack/tool scopes cannot claim framework-owned paths.
acceptance:
  - bwrap uses allowlist mount strategy instead of deny-overlay on full root mount
  - Kernel scope validation rejects reserved .lumenflow/** path scopes for pack/tool-declared writes
  - Security-focused tests cover sandbox mount isolation and reserved scope enforcement
