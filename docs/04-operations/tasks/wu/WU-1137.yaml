id: WU-1137
title: Add mem:export CLI for human-readable memory output
lane: 'Operations: Infrastructure'
type: feature
status: ready
priority: P2
created: 2026-01-26
code_paths:
  - docs/04-operations/_frameworks/lumenflow/agent/onboarding/quick-ref-commands.md
tests:
  manual: []
  unit:
    - packages/@lumenflow/memory/__tests__/mem-export-core.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1137.done
dependencies: []
spec_refs:
  - lumenflow://plans/WU-1137-plan.md
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Add mem:export core + CLI to render memory.jsonl as markdown or JSON with basic filters (wu/type/lifecycle). Expose binary + pnpm script and document in quick-ref commands.
acceptance:
  - mem:export outputs markdown by default and JSON when --format json is provided
  - mem:export supports filtering by --wu and --type (and ignores unmatched filters)
  - quick-ref commands list mem:export
