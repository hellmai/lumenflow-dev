id: WU-2041
title: Add missing spawn-policy-resolver and spawn-lane-occupation modules
lane: 'Framework: Core Lifecycle'
type: bug
status: ready
priority: P2
created: 2026-02-22
code_paths:
  - packages/@lumenflow/core/src/spawn-policy-resolver.ts
  - packages/@lumenflow/core/src/spawn-lane-occupation.ts
tests:
  manual:
    - 'Build succeeds: pnpm turbo run build --filter=@lumenflow/core'
  unit:
    - packages/@lumenflow/core/src/__tests__/wu-spawn.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2041.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: WU-2012 decomposition left spawn-policy-resolver.ts and spawn-lane-occupation.ts unstaged during merge. wu-spawn.ts references these modules but they do not exist on main, causing build failures.
acceptance:
  - pnpm turbo run build --filter=@lumenflow/core succeeds
  - All 85 wu-spawn tests pass
