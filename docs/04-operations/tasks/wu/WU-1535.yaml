id: WU-1535
title: Enable strict TypeScript build configuration
lane: 'Operations: Tooling'
type: refactor
status: ready
priority: P0
created: 2026-02-09
code_paths:
  - tsconfig.build.json
  - packages/@lumenflow/cli/tsconfig.json
  - packages/@lumenflow/core/tsconfig.json
  - packages/@lumenflow/memory/tsconfig.json
  - packages/@lumenflow/initiatives/tsconfig.json
  - packages/@lumenflow/agent/tsconfig.json
  - eslint.config.mjs
tests:
  manual:
    - Run pnpm typecheck and verify strict-mode errors are surfaced and resolved or explicitly tracked.
    - Run pnpm lint and confirm re-enabled security/type rules execute in CI-equivalent mode.
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1535.done
dependencies: []
initiative: INIT-020
phase: 2
spec_refs:
  - lumenflow://plans/polished-frolicking-whale.md
risks: []
notes: 'Track B1 from enterprise hardening initiative. Split from original scope per wu-sizing-guide.md (Oversized -> MUST Split). See also WU-1543 (B2: security lint) and WU-1546 (B3: TS lint).'
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: tsconfig.build.json has strict: false and noEmitOnError: false while IDE config has strict: true (split-brain). Problem: production builds accept code that fails strict checks. Solution: enable strict: true and noEmitOnError: true in tsconfig.build.json and per-package tsconfigs, fix resulting type errors.'
acceptance:
  - 'tsconfig.build.json enforces strict: true and noEmitOnError: true.'
  - Per-package tsconfig files align with strict baseline or have explicit temporary exceptions with justification.
  - pnpm typecheck and pnpm build pass with strict mode enabled.
