id: WU-1819
title: Scaffold Next.js web app with API routes for HTTP surface
lane: 'Content: Site Comms'
type: feature
status: ready
priority: P1
created: 2026-02-18
code_paths:
  - apps/web/
tests:
  manual:
    - pnpm turbo run build succeeds, localhost:3000 serves app
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1819.done
dependencies: []
initiative: INIT-031
phase: 1
blocked_by:
  - WU-1817
spec_refs:
  - .claude/plans/zazzy-growing-cook.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: ui
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Create apps/web/ using Next.js App Router + Tailwind + TypeScript. Wire into pnpm workspace and turbo build graph. API route /api/events/[taskId] creates HTTP surface and streams SSE. API route /api/tasks/[...slug] delegates to REST handlers. Vercel deployment config matching apps/docs/ pattern.
acceptance:
  - apps/web/ exists with Next.js App Router, Tailwind, TypeScript
  - Wired into pnpm-workspace.yaml and turbo build graph
  - API routes delegate to HTTP surface package
  - pnpm turbo run build includes apps/web
