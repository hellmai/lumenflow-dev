id: WU-2064
title: Remove phantom .d.ts types from CLI package.json exports
lane: 'Operations: Tooling'
type: bug
status: ready
priority: P2
created: 2026-02-23
code_paths:
  - packages/@lumenflow/cli/package.json
tests:
  manual:
    - Verify pnpm release --dry-run succeeds after fix
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2064.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: CLI package.json declares types fields (dist/index.d.ts, dist/cli-entry-point.d.ts) but neither build pipeline generates .d.ts files (tsc has declaration:false, tsup has dts:false). Pack validation correctly rejects this contract violation, blocking releases.
acceptance:
  - Pack validation passes for @lumenflow/cli (no missing .d.ts files)
  - Release dry-run succeeds end-to-end
