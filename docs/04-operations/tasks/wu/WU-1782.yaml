id: WU-1782
title: Bound evidence receipt indexing and improve lane-lock cleanup observability
lane: 'Framework: Core Lifecycle'
type: bug
status: ready
priority: P2
created: 2026-02-17
code_paths:
  - packages/@lumenflow/kernel/src/evidence/evidence-store.ts
  - packages/@lumenflow/kernel/src/runtime/kernel-runtime.ts
  - packages/@lumenflow/packs/software-delivery/tool-impl/lane-lock.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/kernel/src/__tests__/evidence-store.test.ts
    - packages/@lumenflow/packs/software-delivery/__tests__/lane-lock.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1782.done
dependencies: []
initiative: INIT-029
phase: 8
spec_refs:
  - /home/tom/.claude/plans/zesty-jumping-pixel.md
risks: []
notes: >-
  (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.


  2026-02-17 spec tighten: codify explicit pruneTask API + runtime hook semantics for testable
  lifecycle cleanup.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Problem: evidence receipt->task map can grow unbounded across long runtimes, and
  lane-lock takeover marker cleanup swallows filesystem errors silently. Add bounded lifecycle
  cleanup and explicit error logging.'
acceptance:
  - EvidenceStore exposes explicit pruneTask(taskId) API that removes receipt->task mappings for a
    completed task.
  - Kernel runtime calls pruneTask(taskId) on task_completed lifecycle handling (not implicit
    garbage collection).
  - Lane-lock takeover marker cleanup logs I/O failures instead of silent catch blocks.
  - Evidence-store tests validate pruneTask behavior directly and verify bounded receipt map growth
    across many completed tasks.
