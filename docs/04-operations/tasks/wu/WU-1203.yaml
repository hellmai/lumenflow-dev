id: WU-1203
title: Add configurable progress signals to memory config
lane: 'Framework: Core'
type: feature
status: ready
priority: P2
created: 2026-01-29
code_paths:
  - packages/@lumenflow/core/src/lumenflow-config-schema.ts
  - packages/@lumenflow/core/src/wu-spawn.ts
  - packages/@lumenflow/cli/src/wu-spawn.ts
  - tools/generate-cli-docs.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/core/src/__tests__/lumenflow-config-schema.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1203.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Add progress_signals configuration section to memory config schema. When enabled, spawn prompts will include mandatory progress signal directives at configurable triggers (milestone completion, tests pass, before gates, when blocked). Frequency-based signals (every N tool calls) also supported. Addresses sub-agent coordination needs without unnecessary token waste.
acceptance:
  - ProgressSignalsConfigSchema added to lumenflow-config-schema.ts with enabled, frequency, on_milestone, on_tests_pass, before_gates, on_blocked, auto_checkpoint fields
  - MemoryConfigSchema extended with optional progress_signals field
  - generateAgentCoordinationSection() in wu-spawn.ts reads config and generates dynamic guidance
  - Spawn prompts show Required-Mandatory when enabled:true, Optional otherwise
  - Schema registered in tools/generate-cli-docs.ts for auto-doc generation
  - pnpm docs:generate produces correct memory.progress_signals documentation
