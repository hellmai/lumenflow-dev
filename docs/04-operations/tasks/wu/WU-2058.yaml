id: WU-2058
title: Harden sub-agent completion prompts with mandatory status pre-check
lane: 'Framework: Core Validation'
type: feature
status: ready
priority: P2
created: 2026-02-23
code_paths:
  - packages/@lumenflow/cli/src/wu-spawn-completion.ts,packages/@lumenflow/cli/src/wu-spawn-prompt-builders.ts
tests:
  manual:
    - Generate WU-2058 brief via pnpm wu:brief --id WU-2058 and verify completion workflow says run
      pnpm wu:status --id WU-2058 before wu:done; if status is done
    - stop and report done without wu:done/wu:recover
  unit:
    - packages/@lumenflow/cli/src/__tests__/wu-spawn-completion.test.ts,packages/@lumenflow/cli/src/__tests__/wu-spawn-prompt-builders.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2058.done
dependencies: []
plan: lumenflow://plans/WU-2058-plan.md
spec_refs:
  - lumenflow://plans/WU-2058-plan.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Add explicit status verification in generated completion workflow so sub-agents do not
  rerun wu:done or invoke wu:recover on already-done WUs.
acceptance:
  - Generated completion prompt instructs running pnpm wu:status --id <WU> from main before wu:done
  - 'Prompt explicitly says: if status=done, stop and report done; do not run wu:done or wu:recover'
  - Prompt keeps autonomous completion behavior when status=in_progress
