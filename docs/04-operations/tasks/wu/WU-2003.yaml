id: WU-2003
title: Detect and heal stale initiative status when all WUs are done
lane: 'Framework: Core Validation'
type: bug
status: ready
priority: P2
created: 2026-02-22
code_paths:
  - packages/@lumenflow/cli/src/state-doctor.ts
  - packages/@lumenflow/cli/src/initiative-status.ts
tests:
  manual:
    - 'Run: pnpm state:doctor --dry-run and verify stale initiatives are reported when status
      mismatches completion'
    - 'Run: pnpm state:doctor --fix and verify stale initiative statuses are set to done'
  unit:
    - packages/@lumenflow/cli/src/__tests__/state-doctor.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2003.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: state:doctor currently reports healthy even when initiative YAML status remains
  in_progress despite 100% WU completion. Add reconciliation so stale initiative status is detected
  and auto-fixable.
acceptance:
  - state:doctor --dry-run reports stale initiative status mismatches
  - state:doctor --fix updates initiative status to done when all linked WUs are done
  - Regression tests cover detection and fix behavior
