id: WU-2183
title: Add workspace root key registry to config-contract
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P2
created: 2026-02-25
code_paths:
  - packages/@lumenflow/core/src/config-contract.ts
tests:
  manual:
    - Verify WORKSPACE_ROOT_KEYS matches WorkspaceSpecSchema fields
    - Verify MANAGED_ROOT_KEYS entries match expected commands
  unit:
    - packages/@lumenflow/core/src/__tests__/config-contract.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2183.done
dependencies: []
initiative: INIT-045
phase: 1
spec_refs:
  - /home/USER/.claude/plans/init-045-multi-pack-config.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: config-contract.ts only has WORKSPACE_V2_KEYS with two entries. Add WORKSPACE_ROOT_KEYS
  (all kernel-owned root keys), WRITABLE_ROOT_KEYS (keys config:set can write directly), and
  MANAGED_ROOT_KEYS (keys that need dedicated commands, with command name). This is the routing
  table for config:set/get rewrite.
acceptance:
  - WORKSPACE_ROOT_KEYS constant lists all kernel-owned root keys
  - WRITABLE_ROOT_KEYS set includes control_plane, memory_namespace, event_namespace
  - MANAGED_ROOT_KEYS maps packs->pack:install, lanes->lane:edit, security->dedicated command
