id: WU-1973
title: Repoint config:set and config:get to workspace.yaml
lane: 'Framework: CLI Enforcement'
type: refactor
status: ready
priority: P0
created: 2026-02-21
code_paths:
  - packages/@lumenflow/cli/src/config-set.ts
  - packages/@lumenflow/cli/src/config-get.ts
  - packages/@lumenflow/cli/src/__tests__/config-set.test.ts
tests:
  manual:
    - pnpm config:get --key software_delivery.methodology.testing returns value from workspace.yaml
  unit:
    - packages/@lumenflow/cli/src/__tests__/config-set.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1973.done
dependencies: []
initiative: INIT-033
phase: 1
blocked_by:
  - WU-1972
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: api
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Update config:set/config:get command handlers to read and write workspace.yaml v2 paths instead of .lumenflow.config.yaml.
acceptance:
  - config:set modifies workspace.yaml keys with safe micro-worktree flow
  - config:get reads workspace.yaml keys and returns normalized values
  - Existing config-set tests are updated for workspace.yaml contract
