id: WU-1310
title: Centralize path defaults in core config (simple/arc42)
lane: 'Framework: Core'
type: refactor
status: ready
priority: P1
created: 2026-02-01
code_paths:
  - packages/@lumenflow/core/src/wu-paths.ts
  - packages/@lumenflow/core/src/lumenflow-config.ts
  - packages/@lumenflow/core/src/lumenflow-config-schema.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/core/src/__tests__/wu-paths.test.ts
    - packages/@lumenflow/core/src/__tests__/path-centralization.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1310.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Complete the core path centralization: expand directories.* defaults to support simple/arc42 structures, ensure WU_PATHS/getResolvedPaths use config values (not hardcoded docs/04-operations), and make config overrides fully respected for core path helpers.'
acceptance:
  - directories.* includes defaults for simple and arc42 structures (wuDir, backlogPath, statusPath, templatesDir, onboardingDir, plansDir, initiativesDir)
  - WU_PATHS/getResolvedPaths use config values for all paths
  - Config overrides are respected by core path helpers
  - Unit tests cover core path defaults and overrides
