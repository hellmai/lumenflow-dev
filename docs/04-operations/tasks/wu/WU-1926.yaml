id: WU-1926
title: Publish lumenflow CLI to npm
lane: 'Framework: CLI Enforcement'
type: feature
status: ready
priority: P1
created: 2026-02-20
code_paths:
  - packages/@lumenflow/cli/package.json
  - packages/@lumenflow/cli/src/index.ts
tests:
  manual:
    - npm install -g from local tarball, run lumenflow --version
  unit:
    - packages/@lumenflow/cli/__tests__/bin-entry-check.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1926.done
dependencies: []
initiative: INIT-031
phase: 8
spec_refs:
  - docs/04-operations/plans/INIT-031-plan.md
risks: []
notes: 'Sizing: MEDIUM (~36 calls, 18 files). Strategy: Single Session.'
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Currently requires clone + pnpm install + turbo build. Target: npm install -g
  @lumenflow/cli. Bundle with esbuild/tsup, fix import.meta.main for Node.js compat (C9).'
acceptance:
  - CLI bundles for standalone use via esbuild or tsup
  - npm pack produces installable package with lumenflow bin entry
  - import.meta.main works in Node.js (not Bun-only)
