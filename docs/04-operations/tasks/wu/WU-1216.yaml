id: WU-1216
title: Add gitignore entries to lumenflow init
lane: 'Framework: CLI'
type: feature
status: ready
priority: P2
created: 2026-01-29
code_paths:
  - packages/@lumenflow/cli/src/init.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1216.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: lumenflow init should append required .lumenflow runtime directory gitignore entries to user's .gitignore (or create if missing). Currently users must manually add these, which leads to dirty repos.
acceptance:
  - lumenflow init appends gitignore entries if missing
  - Entries include recursive ** patterns for nested packages
  - Idempotent - doesn't duplicate entries on re-run
