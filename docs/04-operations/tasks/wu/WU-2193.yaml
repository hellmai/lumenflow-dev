id: WU-2193
title: Align workspace-init defaults with pack pinning semantics
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P3
created: 2026-02-26
code_paths:
  - packages/@lumenflow/cli/src/workspace-init.ts
  - packages/@lumenflow/cli/src/init.ts
tests:
  manual:
    - Run workspace-init in a clean repo and verify packs/software_delivery defaults are coherent
  unit:
    - packages/@lumenflow/cli/src/__tests__/workspace-init.test.ts
    - packages/@lumenflow/cli/src/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2193.done
dependencies: []
initiative: INIT-045
phase: 4
spec_refs:
  - /home/tom/.claude/plans/init-045-multi-pack-config.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'workspace-init currently emits packs: [] with an empty software_delivery block, leaving default workspaces without an explicitly pinned pack despite SD-centric defaults. Align initialization behavior so default workspaces pin software-delivery (or remove SD config stubs) consistently with runtime/config routing expectations.'
acceptance:
  - Default workspace-init output has consistent pack/config semantics (no orphaned software_delivery block)
  - If software_delivery config is emitted by default, software-delivery is pinned by default
  - workspace-init tests cover default output contract for packs + software_delivery
