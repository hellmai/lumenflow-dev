id: WU-1204
title: Add signal TTL cleanup to prevent unbounded growth
lane: 'Framework: Memory'
type: feature
status: ready
priority: P2
created: 2026-01-29
code_paths:
  - packages/@lumenflow/memory/src/signal-cleanup-core.ts
  - packages/@lumenflow/cli/src/signal-cleanup.ts
  - packages/@lumenflow/core/src/lumenflow-config-schema.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/memory/src/__tests__/signal-cleanup-core.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1204.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Signals in .lumenflow/memory/signals.jsonl grow unbounded. Add TTL-based cleanup with configurable retention (7d read, 30d unread, 500 max). Reuse existing mem-cleanup-core.ts patterns.
acceptance:
  - SignalCleanupConfigSchema added to lumenflow-config-schema.ts with ttl, unreadTtl, maxEntries
  - signal-cleanup-core.ts implements cleanupSignals() with TTL and count-based pruning
  - CLI command pnpm signal:cleanup with --dry-run and --ttl options
  - Active WU signals (in_progress/blocked) are always retained
  - Tests cover TTL expiration, count limits, and active WU protection
