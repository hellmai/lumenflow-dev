id: WU-1288
title: Wire methodology policy into wu:spawn CLI output
lane: 'Framework: CLI'
type: bug
status: ready
priority: P1
created: 2026-01-31
code_paths:
  - packages/@lumenflow/cli/src/wu-spawn.ts
  - packages/@lumenflow/cli/__tests__/wu-spawn.test.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/__tests__/wu-spawn.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1288.done
dependencies: []
initiative: INIT-012
phase: 1
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: methodology policy is resolved in core but CLI wu:spawn does not apply it, so config changes have no effect on spawn output. Problem: users set methodology.testing/architecture in .lumenflow.config.yaml yet spawned prompts stay default. Solution: load config + rawConfig in CLI, resolve policy, and ensure wu:spawn output reflects policy-based guidance.'
acceptance:
  - wu:spawn uses resolved methodology policy from project config
  - Spawn output includes correct test/architecture guidance for non-default methodology
  - Default (no methodology config) output remains unchanged
  - CLI tests cover policy-driven spawn output
