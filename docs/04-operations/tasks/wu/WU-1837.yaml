id: WU-1837
title: Extend PackLoader with registry-based pack resolution
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P1
created: 2026-02-18
code_paths:
  - packages/@lumenflow/kernel/src/pack/
tests:
  manual:
    - Configure PackPin with registry source, verify PackLoader resolves it
  unit:
    - packages/@lumenflow/kernel/src/__tests__/pack-loader.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1837.done
dependencies: []
initiative: INIT-031
phase: 4
spec_refs:
  - .claude/plans/zazzy-growing-cook.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Extend PackLoader.load() to handle source: registry. Fetch pack metadata from registry API, download tarball to ~/.lumenflow/pack-cache/, extract, verify integrity, load. PackPin gets optional registry_url field (default: registry.lumenflow.dev).'
acceptance:
  - 'PackLoader resolves packs with source: registry'
  - Tarball downloaded and cached locally
  - Integrity verification runs on registry-fetched pack
  - Default registry_url configurable
