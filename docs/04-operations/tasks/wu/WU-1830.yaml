id: WU-1830
title: Implement AG-UI RunAgent HTTP endpoint in HTTP surface
lane: 'Framework: MCP'
type: feature
status: ready
priority: P1
created: 2026-02-18
code_paths:
  - packages/@lumenflow/surfaces/http/
tests:
  manual:
    - POST to RunAgent endpoint with AG-UI input, verify SSE event stream
  unit:
    - packages/@lumenflow/surfaces/__tests__/ag-ui-runagent.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1830.done
dependencies: []
initiative: INIT-031
phase: 3
blocked_by:
  - WU-1818
spec_refs:
  - .claude/plans/zazzy-growing-cook.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: api
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Implement the standard AG-UI RunAgent HTTP endpoint in the HTTP surface. Accepts AG-UI RunAgentInput (messages, tools, state, context), returns SSE stream of AG-UI events. Maps to kernel: create task from input, claim, execute tools, stream events, complete. This is the standard entry point that CopilotKit and other AG-UI clients expect.'
acceptance:
  - RunAgent endpoint accepts AG-UI RunAgentInput
  - Returns SSE stream of AG-UI events
  - Maps AG-UI input to kernel task lifecycle
  - Compatible with CopilotKit client
