id: WU-1065
title: 'Bug: mem:inbox fails with missing ms dependency - breaks agent monitoring'
lane: 'Framework: CLI'
type: bug
status: ready
priority: P2
created: 2026-01-22
code_paths:
  - packages/@lumenflow/cli/package.json
tests:
  manual:
    - Run pnpm mem:inbox --since 30m and verify no ms dependency error
  unit: []
  e2e: []
artifacts:
  - .beacon/stamps/WU-1065.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "mem:inbox command fails at runtime with 'Missing packages: ms' error despite ms being declared in package.json. The dependency is not being installed to node_modules."
acceptance:
  - mem:inbox --since 30m runs without dependency errors
  - ms package is properly installed when pnpm install runs
