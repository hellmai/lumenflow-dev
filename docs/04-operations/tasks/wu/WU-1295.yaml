id: WU-1295
title: Fix build failure in wu-spawn-context optional mem-context import
lane: 'Framework: Core'
type: bug
status: ready
priority: P1
created: 2026-02-01
code_paths:
  - packages/@lumenflow/core/src/wu-spawn-context.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/core/src/__tests__/wu-spawn-context-integration.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1295.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: Release build failed after WU-1287 due to TypeScript error in wu-spawn-context when casting optional @lumenflow/memory import. Problem: @lumenflow/memory types do not expose generateContext in dist typings, causing TS2352 during core build. Solution: adjust the optional import handling to avoid type mismatch while preserving graceful degradation.'
acceptance:
  - Core build succeeds without TS2352 for optional @lumenflow/memory import
  - wu-spawn-context safely handles missing generateContext export at runtime
