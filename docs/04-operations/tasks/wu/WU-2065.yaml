id: WU-2065
title: Fix release script commit-msg hook incompatibility
lane: 'Operations: Tooling'
type: bug
status: ready
priority: P1
created: 2026-02-23
code_paths:
  - packages/@lumenflow/cli/src/release.ts
  - .husky/hooks/commit-msg.mjs
tests:
  manual:
    - Verify pnpm release --dry-run produces a commit message that matches allowed patterns
  unit:
    - packages/@lumenflow/cli/src/__tests__/release.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2065.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "The release script commits with 'chore: bump all packages to vX.Y.Z' but the commit-msg hook on main only allows WU-prefixed patterns. This causes the commit phase to fail after npm publish, leaving main dirty with uncommitted version bumps."
acceptance:
  - Release script commit message matches an allowed commit-msg hook pattern
  - pnpm release --skip-publish --release-version 3.2.1 completes end-to-end
