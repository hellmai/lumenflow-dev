id: WU-1431
title: Create shared Zod schemas for CLI/MCP parity
lane: 'Framework: Core'
type: feature
status: ready
priority: P2
created: 2026-02-04
code_paths:
  - packages/@lumenflow/core/src/schemas
  - packages/@lumenflow/mcp/src/tools.ts
  - packages/@lumenflow/cli/src/wu-create.ts
tests:
  manual:
    - Add new CLI flag to wu:create, verify MCP tool schema updated automatically
  unit:
    - packages/@lumenflow/core/src/__tests__/schema-parity.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1431.done
dependencies: []
initiative: INIT-MCP-FULL
phase: 1
spec_refs:
  - lumenflow://plans/partitioned-foraging-ocean.md
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: CLI and MCP define argument schemas separately, causing drift. Problem: When
  CLI adds flags (e.g., --notes), MCP tools dont get them automatically. Solution: Create
  @lumenflow/core/schemas with shared Zod schemas that both CLI and MCP import. Add parity test to
  gates.'
acceptance:
  - Shared Zod schemas created in @lumenflow/core/src/schemas/
  - Existing 7 MCP tools migrated to use shared schemas
  - CLI commands (wu:create, wu:claim, wu:done, gates) use shared schemas for validation
  - 'Parity test added: gates fail if CLI options dont match MCP inputSchema'
