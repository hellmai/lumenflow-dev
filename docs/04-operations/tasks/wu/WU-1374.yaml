id: WU-1374
title: Enable enforcement hooks by default for --client claude
lane: 'Framework: CLI'
type: feature
status: ready
priority: P2
created: 2026-02-03
code_paths:
  - packages/@lumenflow/cli/src/init.ts
  - packages/@lumenflow/cli/src/commands/integrate.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/src/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1374.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: Haven 2.8.0 testing showed agent could write anywhere on main without warnings. Problem: Enforcement hooks are opt-in, agent worked entirely on main without claiming WUs. Solution: Make hooks default ON when --client claude is used, add clear warning when writing outside worktree.'
acceptance:
  - lumenflow init --client claude enables enforcement.hooks by default in config
  - Hooks warn when Write/Edit called without active worktree
  - User can opt-out with --no-enforcement flag
