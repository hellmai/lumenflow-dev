id: WU-1763
title: 'Kernel performance: eliminate EventStore per-append reload'
lane: 'Framework: Core Lifecycle'
type: refactor
status: ready
priority: P2
created: 2026-02-17
code_paths:
  - packages/@lumenflow/kernel/src/event-store/index.ts
tests:
  manual:
    - Run EventStore append performance tests; verify append/appendAll maintain ordering and
      durability while avoiding per-append full reload behavior for >100 events.
  unit:
    - packages/@lumenflow/kernel/src/__tests__/event-store.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1763.done
dependencies: []
initiative: INIT-029
phase: 6
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: noreply@anthropic.com
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: EventStore currently reloads the full event log on each append, creating O(n) append
  cost and O(n^2) behavior over longer lifecycles. This WU narrows scope to EventStore append/index
  performance only.
acceptance:
  - EventStore append updates indexes in-memory instead of reloadFromDisk on every append
  - append/appendAll preserve event ordering and durability semantics while avoiding full reload
  - Performance regression coverage includes event append workloads above 100 events
