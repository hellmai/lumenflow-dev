id: WU-1410
title: Add Claude PreToolUse hook for destructive git commands
lane: 'Framework: CLI'
type: feature
status: ready
priority: P2
created: 2026-02-04
code_paths:
  - packages/@lumenflow/cli/src/init.ts
  - packages/@lumenflow/cli/templates/vendors/claude/.claude/hooks/
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/src/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1410.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "Context: Codex bypassed safe-git by running raw 'git worktree remove --force'. Problem: Pre-commit hooks only fire on commits, not on destructive git operations. Solution: Add PreToolUse hook for Claude Code that blocks destructive git patterns at the Bash tool level. Scope: Claude-specific enforcement, scaffolded by lumenflow init --client claude."
acceptance:
  - 'PreToolUse hook blocks: git.*--force, git reset --hard, git clean -fd, git worktree remove'
  - Hook is scaffolded by lumenflow init --client claude
  - Blocked commands show helpful error pointing to safe alternatives (wu:recover, etc)
