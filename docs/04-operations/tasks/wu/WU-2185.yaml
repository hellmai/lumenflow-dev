id: WU-2185
title: Rewrite config:set with workspace-aware routing
lane: 'Framework: CLI WU Commands'
type: feature
status: ready
priority: P2
created: 2026-02-25
code_paths:
  - packages/@lumenflow/cli/src/config-set.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/__tests__/config-set.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2185.done
dependencies:
  - WU-2182
  - WU-2183
initiative: INIT-045
phase: 2
spec_refs:
  - /home/USER/.claude/plans/init-045-multi-pack-config.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'config:set hardcodes software_delivery as write target. Rewrite: remove implicit SD
  prefix, all keys fully qualified from workspace root, route by key prefix (kernel root key ->
  kernel schema, pack config_key -> pack schema, managed key -> error with guidance, unknown -> hard
  error). Write-path strict validation rejects unknown sub-keys.'
acceptance:
  - config:set --key control_plane.sync_interval writes at YAML root
  - config:set --key software_delivery.gates.minCoverage writes under SD block
  - config:set --key gates.minCoverage errors with did-you-mean guidance
  - config:set --key gates.unknown_key errors at write path (strict validation)
  - config:set --key packs.foo errors with use pack:install guidance
