id: WU-1015
title: Add missing CLI command wrappers
lane: CLI
type: feature
status: ready
priority: P0
created: 2026-01-18
code_paths:
  - packages/@lumenflow/cli/src/
tests:
  manual:
    - pnpm build passes in CLI package
    - All bin entries have corresponding source files
  unit: []
  e2e: []
artifacts:
  - .beacon/stamps/WU-1015.done
dependencies: []
risks: []
notes: |
  AUDIT COMPLETED 2026-01-18:
  - @lumenflow/core: 104 files (100% PatientPath tools/lib coverage)
  - @lumenflow/agent: 6 files (all agent-* covered)
  - @lumenflow/initiatives: 6 files (all initiative-* covered)
  - @lumenflow/memory: 12 files (all mem-* covered)

  Only CLI WRAPPERS missing - core logic EXISTS in packages.
requires_review: false
assigned_to: tom@hellm.ai
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: |
  Add missing CLI command wrappers to @lumenflow/cli.
  Core logic already exists in @lumenflow/* packages.

  Missing CLI wrappers (8 total):
  1. wu-delete.ts - safe WU deletion
  2. wu-unlock-lane.ts - lane lock management
  3. orchestrate-initiative.ts - wave-based execution
  4. orchestrate-monitor.ts - execution monitoring
  5. orchestrate-init-status.ts - initiative status
  6. agent-session.ts - start session
  7. agent-session-end.ts - end session
  8. agent-log-issue.ts - log incidents
acceptance:
  - wu-delete and wu-unlock-lane CLI commands added
  - orchestrate-* CLI commands added (3 commands)
  - agent-* CLI wrappers added (3 commands)
  - All bin entries in package.json point to dist files
  - pnpm build passes in CLI package
