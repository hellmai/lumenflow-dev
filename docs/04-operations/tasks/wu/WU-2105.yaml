id: WU-2105
title: 'Fix init workspace.yaml: override INIT-036 path defaults for consumer repos'
lane: 'Framework: CLI WU Commands'
type: bug
status: ready
priority: P1
created: 2026-02-24
code_paths:
  - packages/@lumenflow/cli/src/init.ts
tests:
  manual:
    - Run npx lumenflow on fresh project, verify workspace.yaml paths match scaffolded files
  unit:
    - packages/@lumenflow/cli/src/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2105.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'INIT-036 added 4 new directory config properties (completeGuidePath, quickRefPath, startingPromptPath, governancePath) with OS-repo defaults (docs/04-operations/...). The init command (WU-1755 block at line 550-558) overrides schema defaults to match consumer layout, but missed the 4 new properties. Result: npx lumenflow on a fresh project generates workspace.yaml with paths pointing to docs/04-operations/ which does not exist in consumer repos.'
acceptance:
  - completeGuidePath, quickRefPath, startingPromptPath, governancePath overridden in buildWorkspaceConfig
  - npx lumenflow init generates workspace.yaml with consumer-correct paths
