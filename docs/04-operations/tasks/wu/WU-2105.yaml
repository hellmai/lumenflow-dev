id: WU-2105
title: 'Fix init workspace.yaml: override INIT-036 path defaults for consumer repos'
lane: 'Framework: CLI WU Commands'
type: bug
status: in_progress
priority: P1
created: 2026-02-24
code_paths:
  - packages/@lumenflow/core/src/schemas/directories-config.ts
  - packages/@lumenflow/cli/src/init.ts
  - packages/@lumenflow/cli/src/init-detection.ts
  - workspace.yaml
tests:
  manual:
    - Run npx lumenflow on fresh project, verify workspace.yaml paths match scaffolded files
  unit:
    - packages/@lumenflow/cli/src/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2105.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
claimed_mode: worktree
assigned_to: tom@hellm.ai
claimed_at: 2026-02-24T15:44:04.840Z
worktree_path: /home/USER/source/hellmai/os/worktrees/framework-cli-wu-commands-wu-2105
session_id: 3dd4b47e-0f48-488f-b4fa-ddd5416265ba
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
approved_by:
  - tom@hellm.ai
approved_at: 2026-02-24T15:44:04.845Z
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'INIT-036 added 4 new directory config properties (completeGuidePath, quickRefPath,
  startingPromptPath, governancePath) with OS-repo defaults (docs/04-operations/...). The init
  command (WU-1755 block at line 550-558) overrides schema defaults to match consumer layout, but
  missed the 4 new properties. Result: npx lumenflow on a fresh project generates workspace.yaml
  with paths pointing to docs/04-operations/ which does not exist in consumer repos.'
acceptance:
  - completeGuidePath, quickRefPath, startingPromptPath, governancePath overridden in
    buildWorkspaceConfig
  - npx lumenflow init generates workspace.yaml with consumer-correct paths
  - Schema defaults are consumer-simple, OS workspace.yaml is explicit for all directory paths
  - Init generates correct workspace.yaml for both simple and arc42 layouts
  - 'No override hack: init writes all directory paths explicitly from DocsPathConfig'
