id: WU-2051
title: Extract scm-tools reference domain pack
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P2
created: 2026-02-22
code_paths:
  - packs/scm-tools/
tests:
  manual:
    - Invoke scm-tools tools through kernel tool host
  unit:
    - packs/scm-tools/__tests__/manifest.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2051.done
dependencies: []
spec_refs:
  - .claude/plans/vivid-jumping-twilight.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Extract git-tools, worktree-tools, lane-lock, delegation-tools from software-delivery into standalone packs/scm-tools/ domain pack. Must pass pack:validate all 5 checks including import boundary.
acceptance:
  - packs/scm-tools/ exists with manifest.yaml and tool-impl/
  - pack:validate --pack-root packs/scm-tools passes all 5 checks
  - pack:hash produces stable SHA-256
  - Import boundary scan confirms zero forbidden imports
