id: WU-2223
title: Fix control_plane schema mismatch between core and kernel
lane: 'Framework: Core Validation'
type: bug
status: ready
priority: P1
created: 2026-02-26
code_paths:
  - packages/@lumenflow/core/src/schemas/workspace-config.ts
  - packages/@lumenflow/core/src/lane-checker.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/core/src/__tests__/lane-checker.test.ts
    - packages/@lumenflow/core/src/__tests__/workspace-config.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2223.done
dependencies: []
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'wu:create fails in cloud-connected workspaces because core WorkspaceV2ExtensionsSchema rejects the control_plane block written by cloud:connect and config:set. Core expects old shape (enabled, local_override) while kernel writes new shape (project_id, auth.token_env). Fix: update core schema to match kernel shape, fix misleading FILE_NOT_FOUND error in isValidParentLane.'
acceptance:
  - core WorkspaceControlPlaneConfigSchema matches kernel shape (project_id, auth.token_env)
  - isValidParentLane throws CONFIG_ERROR when workspace.yaml exists but fails validation
  - lane-checker tests cover control_plane + lane validation scenario
  - parity test guards against future schema drift
