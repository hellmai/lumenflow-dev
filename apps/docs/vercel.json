{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "installCommand": "npm install && curl -fsSL https://d2lang.com/install.sh | sh -s -- --dry-run=false",
  "buildCommand": "PATH=\"$HOME/.local/bin:$PATH\" npm run build"
}
