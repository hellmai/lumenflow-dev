import { LoadingSkeleton } from '../../src/components/loading-skeleton';

export default function EvidenceLoading() {
  return (
    <main>
      <LoadingSkeleton label="Loading evidence" />
    </main>
  );
}
