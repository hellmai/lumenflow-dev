import { LoadingSkeleton } from '../../../src/components/loading-skeleton';

export default function PackDetailLoading() {
  return (
    <main>
      <LoadingSkeleton label="Loading pack details" />
    </main>
  );
}
