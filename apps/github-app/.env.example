# LumenFlow GitHub App - Environment Variables
# Copy to .env.local for local development

# =============================================================================
# GitHub App Configuration
# Get these from: https://github.com/settings/apps/YOUR_APP
# =============================================================================

# App ID (number from app settings page)
GITHUB_APP_ID=123456

# Private key (base64 encoded PEM, or raw PEM with newlines)
# Generate in app settings -> Private keys -> Generate a private key
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
...
-----END RSA PRIVATE KEY-----"

# Webhook secret (set in app settings -> Webhook -> Secret)
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# =============================================================================
# Optional: Billing Configuration
# =============================================================================

# For GitHub Marketplace billing (recommended)
# No additional config needed - uses GitHub's API

# For Stripe billing (optional)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# For database (optional - for usage tracking)
# DATABASE_URL=postgres://...

# =============================================================================
# Vercel Configuration
# These are set automatically in Vercel dashboard
# =============================================================================
# VERCEL_GIT_COMMIT_SHA (auto-set)
# VERCEL_ENV (auto-set)
