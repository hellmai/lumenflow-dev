#!/usr/bin/env node
/**
 * Pre-commit hook stub for LumenFlow dogfooding.
 *
 * This validates code before committing.
 * Stub passes for now - actual pre-commit checks via gates.
 */

console.log('[gates-pre-commit] Pre-commit validation (stub - passes)');
process.exit(0);
